# Actual Budget MCP v1 idempotency

This file is generated from the reviewed operation metadata in `src/mcp/idempotency.ts`, the authoritative
definitions in `src/mcp/tool-definitions.ts`, and the verification manifest. Do not edit it by hand.

`idempotent` describes the observable result of repeating the same request after a confirmed success. It does
not authorize blind replay after a timeout, local write failure, synchronization failure, or verification
failure. In those cases, follow the recovery column and inspect state first. `non-idempotent` means an identical
successful call may create another entity, apply another increment, or address identifiers removed by the first
call.

Import preview is intrinsically read-only and has no caller `dryRun` switch: `actual_preview_import` invokes the
official reconciliation pipeline with SDK `dryRun: true`, emits a request fingerprint, performs no sync, and is
covered by `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — proves official import preview purity and binds the reviewed request to execution<br>`test/e2e/write.e2e.test.ts` — previews without mutation, binds fingerprinted import, and verifies lookup/search through compiled stdio.

| Tool | Classification | Successful repeat behavior | Confirmation | Pure preview | Failure recovery | Test evidence |
| --- | --- | --- | --- | --- | --- | --- |
| `actual_close_account` | `idempotent` | Targets the closed state and returns unchanged when the account is already closed. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_create_account` | `non-idempotent` | Every execution may create another account because names are not an idempotency key. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_delete_account` | `non-idempotent` | The first confirmed call removes the account; a repeat addresses a missing identifier. | `confirm-destructive` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_reopen_account` | `idempotent` | Targets the open state and returns unchanged when the account is already open. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_update_account` | `idempotent` | Sets allowlisted account fields to exact desired values and verifies the resulting account. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_copy_budget_month` | `idempotent` | Copies desired planning values; once source and target match, the same request reports no actionable changes. | `budget-copy` | `dryRun` defaults to `true` | Inspect affected/pending IDs, synchronize, and read back before any retry. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — uses dynamically selected clean months for verified budget amount, carryover, hold/reset, and copy writes<br>`test/e2e/write.e2e.test.ts` — executes guarded budget writes and copy through compiled MCP stdio with exact cleanup |
| `actual_hold_budget_for_next_month` | `non-idempotent` | Adds an incremental hold; the same amount is applied again on every successful execution. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — uses dynamically selected clean months for verified budget amount, carryover, hold/reset, and copy writes<br>`test/e2e/write.e2e.test.ts` — executes guarded budget writes and copy through compiled MCP stdio with exact cleanup |
| `actual_reset_budget_hold` | `idempotent` | Resets the manual hold to the same cleared state; repeating does not add another change. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — uses dynamically selected clean months for verified budget amount, carryover, hold/reset, and copy writes<br>`test/e2e/write.e2e.test.ts` — executes guarded budget writes and copy through compiled MCP stdio with exact cleanup |
| `actual_set_budget_amount` | `idempotent` | Sets one category to an exact desired amount; an equal read-back is a no-op. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — uses dynamically selected clean months for verified budget amount, carryover, hold/reset, and copy writes<br>`test/e2e/write.e2e.test.ts` — executes guarded budget writes and copy through compiled MCP stdio with exact cleanup |
| `actual_set_budget_carryover` | `idempotent` | Sets one category to an exact desired carryover flag across the verified month range. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — uses dynamically selected clean months for verified budget amount, carryover, hold/reset, and copy writes<br>`test/e2e/write.e2e.test.ts` — executes guarded budget writes and copy through compiled MCP stdio with exact cleanup |
| `actual_create_category` | `non-idempotent` | Every execution may create another category because name and group are not an idempotency key. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_create_category_group` | `non-idempotent` | Every execution may create another group because its name is not an idempotency key. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_delete_category` | `non-idempotent` | The first confirmed call removes the unused category; a repeat addresses a missing identifier. | `confirm-destructive` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_delete_category_group` | `non-idempotent` | The first confirmed call removes the empty group; a repeat addresses a missing identifier. | `confirm-destructive` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_hide_category` | `idempotent` | Sets hidden to true and returns unchanged when already hidden. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_move_category` | `idempotent` | Sets the category group to one exact compatible destination and verifies it. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_unhide_category` | `idempotent` | Sets hidden to false and returns unchanged when already visible. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_update_category` | `idempotent` | Sets the category name to one exact desired value and verifies it. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_update_category_group` | `idempotent` | Sets the group name to one exact desired value and verifies it. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_import_transactions` | `idempotent` | Required imported_id values drive official reconciliation, so an identical successful import does not duplicate rows. | `none` | None | Inspect affected/pending IDs, synchronize, and read back before any retry. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — imports a uniquely owned transaction and validates manual and imported records with one schema<br>`test/e2e/write.e2e.test.ts` — imports and reads a uniquely owned transaction through MCP stdio |
| `actual_create_payee` | `idempotent` | Returns the single exact existing ordinary payee instead of creating a duplicate for the same normalized name. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — runs the guarded real payee create, read, rename, preflight, and delete lifecycle<br>`test/e2e/write.e2e.test.ts` — runs a restart-safe payee lifecycle through MCP-only calls |
| `actual_delete_payee` | `non-idempotent` | The first confirmed call removes the unused payee; a repeat addresses a missing identifier. | `confirm-destructive` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — runs the guarded real payee create, read, rename, preflight, and delete lifecycle<br>`test/e2e/write.e2e.test.ts` — runs a restart-safe payee lifecycle through MCP-only calls |
| `actual_merge_payees` | `non-idempotent` | The source identifiers cease to exist after success, so the same merge request cannot be replayed. | `confirm-destructive` | None | Inspect affected/pending IDs, synchronize, and read back before any retry. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — merges uniquely owned ordinary payees and remaps an owned source transaction<br>`test/e2e/write.e2e.test.ts` — merges uniquely owned payees with an owned source transaction through MCP-only calls |
| `actual_update_payee` | `idempotent` | Sets one ordinary payee to the exact desired name and verifies it. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — runs the guarded real payee create, read, rename, preflight, and delete lifecycle<br>`test/e2e/write.e2e.test.ts` — runs a restart-safe payee lifecycle through MCP-only calls |
| `actual_create_rule` | `non-idempotent` | Every execution may create another ordered rule because the request has no idempotency key. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — persists, updates, functionally executes, and deletes a uniquely owned rule<br>`test/e2e/write.e2e.test.ts` — creates, updates, functionally executes, and deletes a rule using only MCP tool calls |
| `actual_delete_rule` | `non-idempotent` | The first confirmed call removes the rule; a repeat addresses a missing identifier. | `confirm-destructive` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — persists, updates, functionally executes, and deletes a uniquely owned rule<br>`test/e2e/write.e2e.test.ts` — creates, updates, functionally executes, and deletes a rule using only MCP tool calls |
| `actual_update_rule` | `idempotent` | Applies and verifies the same allowlisted desired full-rule projection. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — persists, updates, functionally executes, and deletes a uniquely owned rule<br>`test/e2e/write.e2e.test.ts` — creates, updates, functionally executes, and deletes a rule using only MCP tool calls |
| `actual_sync` | `idempotent` | Repeating synchronization reconciles the same loaded budget and creates no new entity. | `none` | None | Retry synchronization only after reviewing the sanitized sync error. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — performs an explicit sync and keeps the budget operational<br>`test/e2e/write.e2e.test.ts` — updates, explicitly syncs, and reads the persisted change through MCP |
| `actual_create_schedule` | `non-idempotent` | Every execution may create another schedule because the request has no idempotency key. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — creates, lists, gets, updates, guards, and deletes an isolated schedule<br>`test/e2e/write.e2e.test.ts` — runs an isolated schedule lifecycle and destructive kill-switch check through compiled stdio |
| `actual_delete_schedule` | `non-idempotent` | The first confirmed call removes the schedule; a repeat addresses a missing identifier. | `confirm-destructive` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — creates, lists, gets, updates, guards, and deletes an isolated schedule<br>`test/e2e/write.e2e.test.ts` — runs an isolated schedule lifecycle and destructive kill-switch check through compiled stdio |
| `actual_update_schedule` | `idempotent` | Applies and verifies the same allowlisted desired schedule fields. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — creates, lists, gets, updates, guards, and deletes an isolated schedule<br>`test/e2e/write.e2e.test.ts` — runs an isolated schedule lifecycle and destructive kill-switch check through compiled stdio |
| `actual_bulk_update_transactions` | `idempotent` | Applies exact desired fields; a repeated request classifies already-matching items as unchanged. | `confirm-write` | `dryRun` defaults to `true` | Inspect affected/pending IDs, synchronize, and read back before any retry. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — preflights and executes three heterogeneous bulk updates with exact cleanup and idempotent repeat<br>`test/e2e/write.e2e.test.ts` — runs bulk dry-run, confirmation, heterogeneous execution, exact read-back, and idempotent repeat through compiled stdio |
| `actual_delete_transaction` | `non-idempotent` | The first call removes one transaction or reciprocal pair; a repeat addresses missing identifiers. | `confirm-destructive` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — deletes only the captured, UUID-owned transaction<br>`test/e2e/write.e2e.test.ts` — deletes only the captured transaction after destructive confirmation |
| `actual_update_transaction` | `non-idempotent` | Conservatively not replayable because the SDK does not guarantee safe replay after an ambiguous single or paired transfer update. | `none` | None | Read back the exact entity after an ambiguous write/sync failure before retrying. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — updates only the transaction created by this run and persists notes<br>`test/e2e/write.e2e.test.ts` — updates, explicitly syncs, and reads the persisted change through MCP |
| `actual_create_transfer` | `non-idempotent` | Every execution may create a new reciprocal pair with new identifiers. | `confirm-write` | `dryRun` defaults to `true` | Inspect affected/pending IDs, synchronize, and read back before any retry. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — previews, creates, verifies, owns, and deletes one reciprocal transfer pair<br>`test/e2e/write.e2e.test.ts` — previews, creates, verifies, owns, and deletes one reciprocal transfer through compiled stdio |
