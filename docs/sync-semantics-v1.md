# Actual Budget MCP v1 synchronization semantics

This file is generated from the reviewed read-back metadata in `src/mcp/sync-semantics.ts`, authoritative
tool definitions, and the verification manifest. Do not edit it by hand.

Every v1 mutation-capable tool is either `sync-only` or `write-and-sync`. There are no
`write-without-sync` tools in the frozen 62-tool registry. A successful synchronization observation is not a
claim of rollback or atomicity: if a multi-step operation or a post-write sync fails, callers must use the exact
IDs and recovery guidance returned by the error before attempting another mutation.

| Tool | Sync policy | Read-back expectation | Ambiguous-result recovery | Test evidence |
| --- | --- | --- | --- | --- |
| `actual_close_account` | `write-and-sync` | Reads the exact account and verifies the closed state. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_create_account` | `write-and-sync` | Reads the exact created account, including its observed balance metadata. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_delete_account` | `write-and-sync` | Reads the exact account identifier and verifies that it is absent. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_reopen_account` | `write-and-sync` | Reads the exact account and verifies the open state. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_update_account` | `write-and-sync` | Reads the exact account and verifies the requested name and off-budget state. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_copy_budget_month` | `write-and-sync` | Reads the target month and verifies every attempted category amount and carryover value. | Do not replay. Inspect reported affected, completed, failed, or pending IDs; synchronize, then read back. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — uses dynamically selected clean months for verified budget amount, carryover, hold/reset, and copy writes<br>`test/e2e/write.e2e.test.ts` — executes guarded budget writes and copy through compiled MCP stdio with exact cleanup |
| `actual_hold_budget_for_next_month` | `write-and-sync` | Reads the selected budget month and compares its observed next-month aggregate. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — uses dynamically selected clean months for verified budget amount, carryover, hold/reset, and copy writes<br>`test/e2e/write.e2e.test.ts` — executes guarded budget writes and copy through compiled MCP stdio with exact cleanup |
| `actual_reset_budget_hold` | `write-and-sync` | Reads the selected budget month and reports the observed next-month aggregate. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — uses dynamically selected clean months for verified budget amount, carryover, hold/reset, and copy writes<br>`test/e2e/write.e2e.test.ts` — executes guarded budget writes and copy through compiled MCP stdio with exact cleanup |
| `actual_set_budget_amount` | `write-and-sync` | Reads the exact budget month and category after synchronization. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — uses dynamically selected clean months for verified budget amount, carryover, hold/reset, and copy writes<br>`test/e2e/write.e2e.test.ts` — executes guarded budget writes and copy through compiled MCP stdio with exact cleanup |
| `actual_set_budget_carryover` | `write-and-sync` | Reads the exact budget month and category after synchronization. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — uses dynamically selected clean months for verified budget amount, carryover, hold/reset, and copy writes<br>`test/e2e/write.e2e.test.ts` — executes guarded budget writes and copy through compiled MCP stdio with exact cleanup |
| `actual_create_category` | `write-and-sync` | Reads the exact created category and its persisted group/type projection. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_create_category_group` | `write-and-sync` | Reads the exact created category group. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_delete_category` | `write-and-sync` | Reads the exact category identifier and verifies that it is absent. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_delete_category_group` | `write-and-sync` | Reads the exact category group identifier and verifies that it is absent. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_hide_category` | `write-and-sync` | Reads the exact category and verifies hidden state. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_move_category` | `write-and-sync` | Reads the exact category and verifies its destination group. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_unhide_category` | `write-and-sync` | Reads the exact category and verifies visible state. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_update_category` | `write-and-sync` | Reads the exact category and verifies its name. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_update_category_group` | `write-and-sync` | Reads the exact category group and verifies its name. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — administers isolated account and category structure with exact-ID cleanup and complete safety preflights<br>`test/e2e/write.e2e.test.ts` — exercises the complete structural lifecycle through real MCP stdio |
| `actual_import_transactions` | `write-and-sync` | Returns the official added and updated IDs; callers read those exact IDs to independently verify persisted values. | Do not replay. Inspect reported affected, completed, failed, or pending IDs; synchronize, then read back. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — imports a uniquely owned transaction and validates manual and imported records with one schema<br>`test/e2e/write.e2e.test.ts` — imports and reads a uniquely owned transaction through MCP stdio |
| `actual_create_payee` | `write-and-sync` | Reads the exact existing or newly created ordinary payee. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — runs the guarded real payee create, read, rename, preflight, and delete lifecycle<br>`test/e2e/write.e2e.test.ts` — runs a restart-safe payee lifecycle through MCP-only calls |
| `actual_delete_payee` | `write-and-sync` | Reads the exact payee identifier and verifies that it is absent. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — runs the guarded real payee create, read, rename, preflight, and delete lifecycle<br>`test/e2e/write.e2e.test.ts` — runs a restart-safe payee lifecycle through MCP-only calls |
| `actual_merge_payees` | `write-and-sync` | Returns the target, source, and known affected identifiers; callers read these entities before recovery. | Do not replay. Inspect reported affected, completed, failed, or pending IDs; synchronize, then read back. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — merges uniquely owned ordinary payees and remaps an owned source transaction<br>`test/e2e/write.e2e.test.ts` — merges uniquely owned payees with an owned source transaction through MCP-only calls |
| `actual_update_payee` | `write-and-sync` | Reads the exact ordinary payee and verifies its name. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — runs the guarded real payee create, read, rename, preflight, and delete lifecycle<br>`test/e2e/write.e2e.test.ts` — runs a restart-safe payee lifecycle through MCP-only calls |
| `actual_create_rule` | `write-and-sync` | Reads the exact created rule in official execution order. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — persists, updates, functionally executes, and deletes a uniquely owned rule<br>`test/e2e/write.e2e.test.ts` — creates, updates, functionally executes, and deletes a rule using only MCP tool calls |
| `actual_delete_rule` | `write-and-sync` | Reads the exact rule identifier and verifies that it is absent. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — persists, updates, functionally executes, and deletes a uniquely owned rule<br>`test/e2e/write.e2e.test.ts` — creates, updates, functionally executes, and deletes a rule using only MCP tool calls |
| `actual_update_rule` | `write-and-sync` | Reads the exact rule and compares the allowlisted desired projection. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — persists, updates, functionally executes, and deletes a uniquely owned rule<br>`test/e2e/write.e2e.test.ts` — creates, updates, functionally executes, and deletes a rule using only MCP tool calls |
| `actual_sync` | `sync-only` | Returns only MCP-observed synchronization telemetry; callers read affected domain entities separately. | No domain write is claimed. Review the sanitized sync error, then retry synchronization when appropriate. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — performs an explicit sync and keeps the budget operational<br>`test/e2e/write.e2e.test.ts` — updates, explicitly syncs, and reads the persisted change through MCP |
| `actual_create_schedule` | `write-and-sync` | Reads the exact created schedule from the complete public schedule list. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — creates, lists, gets, updates, guards, and deletes an isolated schedule<br>`test/e2e/write.e2e.test.ts` — runs an isolated schedule lifecycle and destructive kill-switch check through compiled stdio |
| `actual_delete_schedule` | `write-and-sync` | Reads the exact schedule identifier and verifies that it is absent. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — creates, lists, gets, updates, guards, and deletes an isolated schedule<br>`test/e2e/write.e2e.test.ts` — runs an isolated schedule lifecycle and destructive kill-switch check through compiled stdio |
| `actual_update_schedule` | `write-and-sync` | Reads the exact schedule and verifies all requested fields. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — creates, lists, gets, updates, guards, and deletes an isolated schedule<br>`test/e2e/write.e2e.test.ts` — runs an isolated schedule lifecycle and destructive kill-switch check through compiled stdio |
| `actual_bulk_update_transactions` | `write-and-sync` | Reads the exact requested transaction IDs and checks every desired field. | Do not replay. Inspect reported affected, completed, failed, or pending IDs; synchronize, then read back. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — preflights and executes three heterogeneous bulk updates with exact cleanup and idempotent repeat<br>`test/e2e/write.e2e.test.ts` — runs bulk dry-run, confirmation, heterogeneous execution, exact read-back, and idempotent repeat through compiled stdio |
| `actual_delete_transaction` | `write-and-sync` | Reads the deleted transaction and known reciprocal counterpart to verify absence. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — deletes only the captured, UUID-owned transaction<br>`test/e2e/write.e2e.test.ts` — deletes only the captured transaction after destructive confirmation |
| `actual_update_transaction` | `write-and-sync` | Reads the exact transaction and, when applicable, its reciprocal transfer relationship. | The local write may have succeeded. Do not replay until explicit sync and exact read-back establish state. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — updates only the transaction created by this run and persists notes<br>`test/e2e/write.e2e.test.ts` — updates, explicitly syncs, and reads the persisted change through MCP |
| `actual_create_transfer` | `write-and-sync` | Searches the bounded creation candidate set and verifies both reciprocal transaction IDs. | Do not replay. Inspect reported affected, completed, failed, or pending IDs; synchronize, then read back. | `test/mcp.test.ts` — executes every handler successfully with structured and JSON text results<br>`test/integration/write.integration.test.ts` — previews, creates, verifies, owns, and deletes one reciprocal transfer pair<br>`test/e2e/write.e2e.test.ts` — previews, creates, verifies, owns, and deletes one reciprocal transfer through compiled stdio |
